PORT=8787
ALLOWED_ORIGINS=https://drjasofficial.github.io,http://127.0.0.1:4173,http://localhost:4173
ADMIN_USER=jas
ADMIN_PASSWORD=replace-with-a-long-random-password
IP_HASH_SECRET=replace-with-a-different-long-random-secret
TRUST_PROXY=true
# Full IP storage is optional. Keep false unless you genuinely need it and your
# privacy notice/retention policy covers it. A stable private IP hash is always stored.
STORE_FULL_IP=false
